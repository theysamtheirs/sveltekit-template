# Development: no .env needed — falls back to file:local.db automatically.
# Run `npm run db:push` once to create the local schema.

# Production: set both variables in your hosting environment.
DATABASE_URL="libsql://db-name-user.turso.io"
DATABASE_AUTH_TOKEN=""
